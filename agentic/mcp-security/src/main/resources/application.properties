spring.application.name=mcp-security

# JWT issuer URI — must match the iss claim in tokens issued by your authorization server.
# Example: https://your-auth-server.example.com
spring.security.oauth2.resourceserver.jwt.issuer-uri=
